package com.sheryians.major.service;

import org.springframework.stereotype.Service;

@Service
public class CartService {

    public double calculateTotalAmount() {
        // Assume you have access to the cart and products
        double totalAmount = 0.0;

        // Logic to calculate the total amount from the cart items
        // For example, summing up the prices of each item in the cart

        return totalAmount;
    }
}

